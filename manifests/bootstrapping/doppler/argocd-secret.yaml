apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: argocd
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: argocd-token
  managedSecret:
    name: argocd-secret
    namespace: argocd