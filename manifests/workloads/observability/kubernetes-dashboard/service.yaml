apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard
spec:
  ports:
    - port: 8443
      protocol: TCP
      targetPort: 8443
      nodePort: 32201
  selector:
    app: kubernetes-dashboard
  sessionAffinity: None
  type: NodePort