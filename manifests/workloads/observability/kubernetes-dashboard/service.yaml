apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard
spec:
  ports:
  - protocol: TCP
    port: 8443
    targetPort: 8443
    nodePort: 32400
  selector:
    app: kubernetes-dashboard
  type: NodePort