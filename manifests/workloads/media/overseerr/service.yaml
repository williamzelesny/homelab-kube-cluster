apiVersion: v1
kind: Service
metadata:
  name: overseerr
spec:
  type: LoadBalancer
  ports:
    - port: 5055
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: overseerr
    app.kubernetes.io/instance: overseerr